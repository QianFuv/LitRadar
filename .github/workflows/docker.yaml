name: Build and Push Docker Images

on:
  push:
    branches: [main]
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/**"
      - "libs/**"
      - "data/meta/**"
      - "Dockerfile"
      - "app/**"
      - ".github/workflows/rust.yaml"
      - ".github/workflows/frontend.yaml"
      - ".github/workflows/docker.yaml"

env:
  REGISTRY: ghcr.io

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      api_quality: ${{ steps.filter.outputs.api_quality }}
      app_quality: ${{ steps.filter.outputs.app_quality }}
      api_image: ${{ steps.filter.outputs.api_image }}
      app_image: ${{ steps.filter.outputs.app_image }}
    steps:
      - uses: actions/checkout@v6

      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            api_quality:
              - "Cargo.toml"
              - "Cargo.lock"
              - "crates/**"
              - "libs/**"
              - "data/meta/**"
              - "Dockerfile"
              - ".github/workflows/rust.yaml"
            app_quality:
              - "Cargo.toml"
              - "Cargo.lock"
              - "crates/ps-api/**"
              - "crates/ps-domain/**"
              - "app/**"
              - ".github/workflows/frontend.yaml"
            api_image:
              - "Cargo.toml"
              - "Cargo.lock"
              - "crates/**"
              - "libs/**"
              - "data/meta/**"
              - "Dockerfile"
            app_image:
              - "app/**"

  build-api:
    needs: [changes, rust-quality]
    if: needs.changes.outputs.api_image == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}-api
          tags: |
            type=sha
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-app:
    needs: [changes, frontend-quality]
    if: needs.changes.outputs.app_image == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository }}-app
          tags: |
            type=sha
            type=raw,value=latest

      - uses: docker/build-push-action@v6
        with:
          context: ./app
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  rust-quality:
    needs: changes
    if: needs.changes.outputs.api_quality == 'true'
    uses: ./.github/workflows/rust.yaml

  frontend-quality:
    needs: changes
    if: needs.changes.outputs.app_quality == 'true'
    uses: ./.github/workflows/frontend.yaml
