'use client';

/**
 * Temporary weekly-updates route wrapper retained until root-view consolidation.
 */

import { WeeklyUpdatesView } from '@/components/weekly/weekly-updates-view';

/**
 * Render the reusable weekly-updates workspace view.
 *
 * @returns Weekly-updates workspace.
 */
export default function WeeklyUpdatesPage() {
  return <WeeklyUpdatesView />;
}
