import { AdminWorkspace } from '@/components/desktop/admin-workspace';

/**
 * Render the protected admin page.
 *
 * @returns Admin page.
 */
export default function AdminPage() {
  return <AdminWorkspace />;
}
