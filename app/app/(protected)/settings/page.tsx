import { SettingsWorkspace } from '@/components/desktop/settings-workspace';

/**
 * Render the account settings route.
 *
 * @returns Account settings page.
 */
export default function SettingsPage() {
  return <SettingsWorkspace />;
}
