import { FavoritesWorkspace } from '@/components/desktop/favorites-workspace';

/**
 * Render the favorites route.
 *
 * @returns Favorites page.
 */
export default function FavoritesPage() {
  return <FavoritesWorkspace />;
}
