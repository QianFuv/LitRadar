import { TrackingWorkspace } from '@/components/desktop/tracking-workspace';

/**
 * Render the tracking route.
 *
 * @returns Tracking page.
 */
export default function TrackingPage() {
  return <TrackingWorkspace />;
}
